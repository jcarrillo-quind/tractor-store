spring:
  application:
    name: tractor-store
  profiles:
    active: dev

server:
  servlet:
    context-path: /

tractor-store:
  cors:
    allowed-origins:
      - http://localhost:4200
      - http://127.0.0.1:4200
